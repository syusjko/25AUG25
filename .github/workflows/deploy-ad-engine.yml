# .github/workflows/deploy-ad-engine.yml
name: Deploy Ad Engine Service

on:
  push:
    branches:
      - main
    paths:
      - 'services/ad-engine/**' # 이 경로에 변경이 있을 때만 실행
  workflow_dispatch: # 수동 실행 기능

jobs:
  deploy:
    name: Deploy to AWS
    uses: ./.github/workflows/reusable-terraform-deploy.yml
    with:
      service_path: 'services/ad-engine'
    secrets:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      TF_VARS: ${{ secrets.AD_ENGINE_TF_VARS }}
